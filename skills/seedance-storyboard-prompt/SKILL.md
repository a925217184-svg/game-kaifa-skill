---
name: seedance-storyboard-prompt
description: 把粗略情节剧本/一句话想法补全为 Seedance 2.0 可直接跑的分镜脚本与提示词，并从 424 条电影技巧库里选型优化镜头语言。适用触发词：写 Seedance 提示词、补全分镜、买量视频分镜、动态漫分镜、三国/国风 3D Q 版买量广告分镜、帮我优化这个想法/镜头怎么拍/这里用什么光。三阶段流程：阶段零想法优化提案（等确认）→ 阶段一完整分镜脚本（等确认）→ 阶段二拼装提示词；参考图未生成时一律留空占位。
agent_created: true
version: 1.3
sources:
  spec: F:\AI视频制作\outputs\Seedance2.0_提示词规范_v1.0.md
  expression_library: F:\AI视频制作\outputs\人物表情描述库_v1.md
  cinematic_library: C:\Users\Administrator\.workbuddy\skills\seedance-storyboard-prompt\references\cinematic-techniques.md
  cinematic_library_json: C:\Users\Administrator\.workbuddy\skills\seedance-storyboard-prompt\references\cinematic-techniques.json
  intent_map: C:\Users\Administrator\.workbuddy\skills\seedance-storyboard-prompt\references\intent-to-technique.md
  search_tool: C:\Users\Administrator\.workbuddy\skills\seedance-storyboard-prompt\references\_tools\search.py
  library_source: https://melies.co/cinematic-techniques
  prior_work: F:\AI视频制作\outputs\三国吹牛系统_分镜脚本与Seedance提示词_参考图锁定.md
---

# Seedance 分镜提示词流水线

## 开场必读

1. 先读真源规范：`F:\AI视频制作\outputs\Seedance2.0_提示词规范_v1.0.md`（本 skill 只是流程索引，规范是唯一真源）。
2. **表情优先筛选**：先读 `outputs/人物表情描述库_v1.md`，从 31 条标准词条里挑最贴近的一条引用，写作「表情【词条名】」；**严禁自造近义词**；库内无贴合词条才标「表情【自定义：…】（待确认）」并与用户沟通。
3. **电影技巧优先查库**：`references/cinematic-techniques.md`（或同名 .json）收录 melies.co 全站 **424 条技巧**（13 类：镜头运动 86 / 构图景别 25 / 角度 19 / 灯光 41 / 构图 32 / 镜头光学 17 / 色彩胶片 19 / 时间运动 21 / 机内特效 57 / 剪辑转场 23 / 氛围天气 13 / 类型风格 27 / 病毒风格 44），每条含别名、定义、叙事功能、怎么拍、何时用/不用、近似对比、片中实例、**Prompt 模板**、示例 Prompt、常见错误。**选镜头语言/灯光/构图/风格时先查本库挑词条**，选中的把 Prompt 模板翻译/本地化进 Shot 的「镜头」「光影」字段；库内无贴合再自造，并注明。词条正文为英文原文，直接引用 Prompt 时可整段英文嵌入中文提示词。
   - **检索方式（二选一）**：① 跑脚本（推荐，输出精简）`python C:\Users\Administrator\.workbuddy\skills\seedance-storyboard-prompt\references\_tools\search.py 压迫感 --n 6`，支持中文意图词/英文关键词/`--cat` 限定分类/`--name "xxx"` 取全文/`--full` 全字段/`--intent` 列意图词；② 直接 Grep `cinematic-techniques.md`。**不要整篇 Read 这个 1.3MB 文件**。
   - **感觉词 → 专业词条**：先查 `references/intent-to-technique.md`（意图映射表，含情绪/场面/时间/光线/风格五类 + 组合配方），把"压迫感""燃""反转要狠"翻译成具体词条，再用上面的检索脚本取全文。

## 阶段零 · 想法优化（用户给的是粗略想法/一句话/毛坯脚本时必走）

当用户给的很粗（一句话、几句情节、无镜头语言、无光线设计），**不要直接进阶段一**，先做优化提案，等用户选定后再展开成脚本。

**5 步：**

1. **拆意图**：从原文里抽出情绪/场面/节奏/风格四类意图。用户没写的，按剧情类型**合理推断**并标注「（推断）」，不自作主张改剧情。
2. **查库选型**：每个意图 → `intent-to-technique.md` 找候选 → `search.py` 取定义与 Prompt 模板。每类给 **2–3 个候选**，不要只给一个。
3. **出提案表**（格式固定）：

   | 意图（原文/推断） | 候选技巧 | 推荐 | 理由 | 对原想法的具体改写 |
   |---|---|---|---|---|

   推荐项要写清**为什么选它、放弃了什么**（例："Dolly Zoom 比 Crash Zoom 更贴'认知崩塌'，后者偏喜剧 sting"）。
4. **给改写对照**：把用户原文和"套用技巧后"的写法并列，让他一眼看出变化。**只在镜头语言/光线/节奏层面改写，不改剧情和台词**。
5. **等确认**：用户拍板（或说"你定"）后才进阶段一展开完整脚本。

**硬约束**：一镜只用**一种主导镜头运动**；情绪靠「运动 + 角度 + 光 + 景别」组合，不靠堆词；引用模板前扫一眼该词条的「何时不用/常见错误」，少数模板段落是反例。

## 口播驱动分镜（用户给了配音/口播音频文件时，必走）

**不要凭感觉估每句口播的时长，先量化断句。** 拿到音频路径后第一步跑静音检测：

```bash
ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1 "路径.mp3"
ffmpeg -hide_banner -nostats -i "路径.mp3" -af silencedetect=noise=-32dB:d=0.18 -f null - 2>&1 | grep -E "silence_(start|end)"
# 停顿太短没检出来时，放细一档：noise=-38dB:d=0.10
```

拿到每个语音段的起止秒数后：

1. **先建「音频节拍对照表」**（语音段 / 口播原文 / 起–止 / 时长），写进脚本文件——这是分镜时间轴的唯一依据，也是后面剪辑对齐用的表。
2. **字幕数校验**：`中文字数 ÷ 语音时长` 应在 **4.5–7 字/秒**。若某段算出来远超 7 字/秒，说明你**把两句话并成了一段**（漏检了中间的短停顿），回去放细阈值重测；反之若低于 3 字/秒，说明停顿被当成了语音段。
3. **切镜规则**：镜头边界优先落在**静音区间**里；用户要求「尽可能多切」时，可以在**同一语音段内部**按动作另加切点（一个语音段拆 2–3 镜），但单镜**不短于 0.5 秒**。
4. **插入镜（0.5–0.8s）**在备注里注明「可从相邻镜的同批素材里裁，不单独跑生成」，避免浪费生成额度。
5. **口播是旁白还是台词**要问清：若是画外音旁白（第一人称"当年我…"之类），**画面内人物一律不开口、不做口型**，否则口型会和旁白打架；同期声字段只写环境音与动作音效，人声由后期贴。
6. 口播本身是已录好的音频时，**成片通常无 BGM**（口播独占声场），同期声只留音效。

## 怎么调用

三种方式任选，效果一样：

1. **斜杠命令**：`/seedance-storyboard-prompt`
2. **说人话触发**：「写 Seedance 提示词」「补全分镜」「买量视频分镜」「帮我优化这个想法」「这里镜头怎么拍」「这里用什么光」
3. **连想法一起丢**（推荐，最省事）：

```
/seedance-storyboard-prompt

想法/剧情：（一句话或几句情节，越粗越好，细节我来补）
想要的感觉：（如"要压迫感""这里要燃""反转要狠" —— 不写我就按剧情推断并标注）
风格：（不写套默认 3D Q 版次世代国风）
时长/镜数：（不写默认 5s 一镜）
画幅：（不写套本片定稿 9:16 竖版）
参考图：（有就给路径，没有就留空，我写「待补」占位）
```

## 两阶段流程（不跳步）

**阶段一 · 完整分镜脚本（先交付，等用户确认）**
1. 全局风格块（无指定则套默认：3D Q 版次世代国风 + PBR + 电影级游戏 CG）。
2. 资产清点建锚定表；**参考图没生成的位置写「（参考图：待补）」占位**，图到位后只回填编号。
3. 切分镜头：**一 Shot = 一镜 = 1 个主事件**；装不下就拆，编号顺延、时间轴接续。禁止镜内切镜。**用户给了口播/配音音频时，先按上方「口播驱动分镜」跑静音检测再切镜**，别凭感觉估时长。
4. 每镜：焦段 + 景别 + **景深与焦点（浅景深必须写焦点锁谁）** + 运动 + 机位数值（位置/离地高度/朝向）。
5. 站位（画面空间顺序）+ 光照（时段+光源+方向+光质+氛围，开放式电影灯光描述）+ 动作微表情（**表情库优先筛选，一律写「表情【词条名】——眼部描述；嘴角描述；面部描述」双写格式，禁止只写词条名**）+ 台词。
6. 输出完整脚本，交用户确认。

**阶段二 · 提示词生成（确认后）**
7. 回填参考图编号，更新锚定表。
8. 按规范 §4 公式拼装（表情在阶段一已双写，直接复用展开描述），**每个 Shot 前带实例化的「全局锁段」**（风格四要素 + 资产锚定声明句，见规范 §9.3），全局锁 + Shot 正文连成一体可复制（见下方硬规则）。
9. 同期声 + 全局约束校验，输出。

## 硬规则（踩过坑的）

- **【表情全流程双写，禁止【词条名】裸奔】所有交付物（阶段一分镜脚本、阶段二提示词）里，表情一律写「词条名 + 展开描述」**：`表情【极致震惊】——双眼骤然睁大瞪圆、瞳孔收缩、眉头高挑上扬；嘴唇微张、嘴角向两侧平拉发僵、下颌微垂；额肌收紧、面部肌肉僵住、血色略褪`。展开保留「眼部 + 嘴角 + 面部」三个维度，可压成一句连贯中文但三维度都要在。**任何地方出现孤零零的【词条名】都算违规**——模型不认词条名，用户也不该再手动展开一次（2026-09-20 二次纠正：旧规则允许脚本只写简称，导致脚本直接被拿去用时模型读不懂）。
- **【最高优先级】交付 = 「全局锁段 + Shot 正文段」，两段紧挨、一次全选可复制**。全局锁段含：风格串（题材/材质/ cinematic game CG, PBR material texture）+ 基调递进链（→ 连接 ≤5 词）+ 一句话色调 + 光线设计总述 + **资产锚定声明句**（按规范 §2.2 四种模板逐条实例化，参考图未生成写「（图片 N：待补）」）。**锚定句只住全局锁，绝不写进「拍摄内容」正文**；同一批 Shot 共用同一份全局锁、逐 Shot 重复携带（Seedance 每次生成独立）。风格表/锚定表只作查阅，**不能只给表格不给锁**——全局风格块和锚定模板是提示词组成部分，必须实例化成可复制文字（2026-09-20 用户纠正）。参考图清单、备注、风险提示挪到文件末尾独立小节；正文块内禁止插给人看的内容。
- 机位必须数值化（"摄影机位于 X，离地 N 米"），禁止模糊位置。
- 5s 一档；超 1 主事件拆 Shot，不硬塞。
- 系统弹窗文字 = 画面内 UI，写"清晰显示白色文字"+ 文案原文照抄。
- 配角情绪按递进链推进：嘲讽冷怒 → 轻柔错愕 → 极致震惊 → 慌张惊恐 → 麻木绝望（配角的反应线 = 主角的爽感刻度尺）。
- 循环/卡点镜头走首尾帧同图锁定；叙事推进镜头不锁。
- 默认参数：seedance2.0fast_vip / 5s / 每镜 1–4 张参考图。
- 用户发长图（如表情参数表）会被压成窄条不可读 → 要文件路径，本地 PIL 竖向切片 2000px/片后逐段读取。

## 待用户拍板（悬置项，每次开工前确认）

画幅（16:9 vs 9:16）· 音频（音画同生 vs 后期 TTS）· 弹窗生成方式 · 参考图命名。

## 本项目已定稿参数（《逆天吹牛系统》，可直接沿用）

- 画幅 **9:16 竖版**；音频**音画同生**；系统弹窗**画面内直接生成**，文字由**机械音色画外音**朗读；模型 `seedance2.0fast_vip`（大场面镜换 `seedance2.0`）。
- 竖版构图铁律：多人同镜改纵深/前后排布；大全景纵向延伸；低角度大场面运动向上出画；落版主体占上 2/3、底部留 1/3 净空给 CTA；弹窗放人物下方/侧下方不遮脸。
- 当前主线：**12 镜 / 50.0s**，提示词 `outputs/逆天吹牛系统_Seedance提示词_v2.4_可复制段落版_50秒12镜.md`。
